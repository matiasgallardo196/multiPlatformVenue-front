"use client"

// Side-effect import to install the fetch logger in the browser
import "@/lib/fetch-logger"

export function FetchLogger() {
  // This component renders nothing; it just ensures the logger runs on the client
  return null
}

